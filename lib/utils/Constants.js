const USER_PREFIX = 'earth.user';
const TOKEN_PREFIX = 'earth.token';
const FutureBureau_PREFIX = 'earth.futureBureau';

const USER_ROLES = ['admin', 'user'];


module.exports = {
  USER_PREFIX,
  TOKEN_PREFIX,
  USER_ROLES,
  FutureBureau_PREFIX,
};
